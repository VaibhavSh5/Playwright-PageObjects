{
    "default": {
        "paths": ["Features/*.feature"],
        "require": ["Features/step-definitions/*.ts",
                    "Features/hooks/hooks.ts"],
        "requireModule": ["ts-node/register"],
        "format": ["pretty",
                    "progress", "json:reports/cucumber-report.json"]
    }
}